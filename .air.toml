root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/main ./cmd/api"
entrypoint = "tmp/main"
include_ext = ["go"]
include_dir = ["cmd", "internal"]
exclude_dir = ["tmp"]
delay = 1000
stop_on_error = true
send_interrupt = true
kill_delay = 500

[log]
time = true
